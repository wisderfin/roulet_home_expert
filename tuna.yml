tunnels:
  frontend:
    frontend: [frontend]
    commandLine: tuna http frontend:5173 --ua-allow=Chrome --subdomain=slmnfrontend
  backend:
    tags: [backend]
    commandLine: tuna http backend:8000 --subdomain=locback
