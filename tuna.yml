tunnels:
  frontend:
    frontend: [frontend]
    commandLine: tuna http frontend:5173 --subdomain=locfront

  backend:
    tags: [backend]
    commandLine: tuna http backend:8000 --subdomain=locback
