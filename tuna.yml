tunnels:
  frontend:
    frontend: [frontend]
    commandLine: tuna http frontend:5173 --ua-allow=Chrome --subdomain=locfront
  backend:
    tags: [backend]
    commandLine: tuna http backend:8000 --subdomain=locback
  pgadmin:
    tags: [pgadmin]
    commandLine: tuna http pgadmin:80 --subdomain=locpgadm
