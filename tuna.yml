tunnels:
  frontend:
    frontend: [frontend]
    commandLine: tuna http frontend:4173 --subdomain=locfront --location=nl

  backend:
    tags: [backend]
    commandLine: tuna http backend:8000 --subdomain=locback --location=nl
