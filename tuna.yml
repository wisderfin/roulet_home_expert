tunnels:
  frontend:
    frontend: [frontend]
    commandLine: tuna http frontend:4173 --ua-allow=Chrome --subdomain=locfront

  backend:
    tags: [backend]
    commandLine: tuna http backend:8000 --subdomain=locback
